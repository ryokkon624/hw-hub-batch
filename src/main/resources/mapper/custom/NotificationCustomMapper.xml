<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hwhub.batch.infrastructure.mybatis.custom.mapper.NotificationCustomMapper">

  <insert id="bulkInsert">
    INSERT INTO t_notification (
        household_id
        , notification_type
        , actor_user_id
        , target_user_id
        , is_read
        , read_at
        , title_key
        , body_key
        , params_json
        , link_type
        , link_id
        , occurred_at
        , aggregated_key
        , aggregated_count
        , create_user_id
        , create_program
        , update_user_id
        , update_program
    )
    VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.householdId},
                #{item.notificationType},
                #{item.actorUserId},
                #{item.targetUserId},
                #{item.isRead},
                #{item.readAt},
                #{item.titleKey},
                #{item.bodyKey},
                #{item.paramsJson},
                #{item.linkType},
                #{item.linkId},
                #{item.occurredAt},
                #{item.aggregatedKey},
                #{item.aggregatedCount},
                #{item.createUserId},
                #{item.createProgram},
                #{item.updateUserId},
                #{item.updateProgram}
            )
        </foreach>
  </insert>

</mapper>
