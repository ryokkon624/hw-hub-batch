<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hwhub.batch.infrastructure.mybatis.custom.mapper.HouseworkTaskRecalcRequestCustomMapper">

    <!-- PENDINGを取得 -->
    <select id="selectPendingRequests"
            resultType="com.hwhub.batch.domain.model.HouseworkTaskRecalcRequest">
        SELECT
        request_id    AS requestId,
        housework_id  AS houseworkId
        FROM
        t_housework_task_recalc_request
        WHERE
        recalc_request_status = '0'
        ORDER BY
        created_at ASC
        LIMIT #{limit}
    </select>

    <!-- DONE 更新 -->
    <update id="updateStatusToDoneByRequestIds">
        UPDATE t_housework_task_recalc_request
        SET
        recalc_request_status = '2'
        , last_error_message = NULL
        , update_user_id = #{userId}
        , update_program = #{program}
        WHERE
        recalc_request_status = '0'
        AND request_id IN
        <foreach collection="requestIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- FAILED 更新 -->
    <update id="updateStatusToFailedByRequestIds">
        UPDATE t_housework_task_recalc_request
        SET
        recalc_request_status = '9',
        retry_count     = retry_count + 1,
        last_error_message = #{message},
        update_user_id  = #{userId},
        update_program  = #{program}
        WHERE
        recalc_request_status = '0'
        AND request_id IN
        <foreach collection="requestIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>
</mapper>
